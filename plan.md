# План проекта: Детекция шлемов (Sprint 4)

## Текущий статус
- [x] Данные скачаны и изучены (EDA).
- [x] Окружение на **GTX 1080 Ti** исправлено (PyTorch 2.2.0+cu118).
- [x] Разметка сгенерирована через Qwen2.5-VL (T4/Colab + Тест на 1080 Ti).
- [x] Датасет сконвертирован в формат YOLO (`data/yolo_dataset`).

---

## Этапы реализации

### Этап 1: Окружение и данные (ЗАВЕРШЕНО)
- Основная машина: GTX 1080 Ti (sm_61).
- Исправлена поддержка CUDA.
- Данные готовы к обучению.

### Этап 2: Генерация разметки (ЗАВЕРШЕНО)
- Использована модель `Qwen/Qwen2.5-VL-3B-Instruct`.
- Сформирован `vqa_labels_t4.json`.

### Этап 3: Подготовка YOLO (ЗАВЕРШЕНО)
- Скрипт `phase-4-convert-to-yolo.py` разбил данные на 800 (train) и 201 (val).
- Созданы `.txt` аннотации и `data.yaml`.

### Этап 4: Обучение модели (ТЕКУЩИЙ)
- **`phase-5-train-yolo.py`**:
    - Модель: YOLOv8 (Ultralytics).
    - Трекинг: MLFlow (`http://188.243.201.66:5000/`).
    - Параметры: `epochs=50`, `imgsz=416`.

### Этап 5: Оценка и финальный отчет
- Запуск `phase-6-evaluate.py`.
- Сборка всего кода в итоговый Jupyter Notebook.

---

## Архитектурные решения
1.  **Локальная 1080 Ti**: Используется для обучения YOLO и инференса (Transformers).
2.  **MLFlow**: Централизованное хранение метрик обучения.
3.  **VLM Fallback**: Из-за несовместимости `vllm-v1` с Pascal, локально используется `transformers`.
